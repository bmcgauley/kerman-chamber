# Phase 2.5 — KCC UI/UX Audit
**Date:** 2026-02-21 | **Tool:** UI/UX Pro Max (uipro-cli) + hands-on component audit

This audit was generated by running UI/UX Pro Max design-system, color, and UX searches against the current KCC template, then cross-referencing with a hands-on review of all components. Findings are grouped into **themes** with a prioritized task checklist.

---

## 🎨 Color System — **PASS (with minor gaps)**

UI/UX Pro Max confirms our palette matches top authority/hospitality tiers:

| Role | Current | Standard Match | Status |
|------|---------|---------------|--------|
| Primary | `#1A2E4A` (navy) | Hotel/Legal `#1E3A8A` | ✅ Correct tier |
| Accent/CTA | `#C8971A` (gold) | Hospitality `#CA8A04` | ✅ Correct tier |
| Background | `#F8F4EE` (warm white) | Standard `#F8FAFC` | ✅ |
| Body text | `text-slate-600` | — | ⚠️ Not in design token |
| Secondary text | `text-slate-500` | — | ⚠️ Not in design token |

### Tasks
- [ ] **2.5-A1** Add `--color-text-body` and `--color-text-muted` tokens to `globals.css @theme` to replace raw `text-slate-*` classes throughout
- [ ] **2.5-A2** Verify WCAG 4.5:1 contrast: gold `#C8971A` on white `#F8F4EE` (currently borderline)
- [ ] **2.5-A3** Verify WCAG 4.5:1 contrast: white text on `bg-primary/80` hero overlay

---

## 🔤 Typography — **PASS (with consistency gaps)**

Current stack: `Playfair Display` (display/headings) + `Inter` (body) via `next/font`. UI/UX Pro Max recommended `Rubik + Nunito Sans` for community landing — but our serif pair is more appropriate for a civic/authority brand.

### Tasks
- [ ] **2.5-B1** Add `font-display` to ALL section `<h2>` headings (currently `page.js` L33, L72, L106 use it inconsistently)
- [ ] **2.5-B2** Standardize heading scale: h1=7xl, h2=5xl, h3=3xl — currently mixed (`text-4xl md:text-5xl` vs `text-3xl md:text-4xl`)
- [ ] **2.5-B3** Add `--font-size-*` tokens to `@theme` for reuse (rather than arbitrary Tailwind scale classes)

---

## 📱 Mobile / Responsive — **CRITICAL GAPS**

UI/UX Pro Max requires: 375px · 768px · 1024px · 1440px verified.

Hands-on review found:

| Issue | Location | Severity |
|-------|---------|---------|
| Mobile nav hamburger opens nothing | `Header.jsx` L70-77 | 🔴 Critical |
| Hero `h-[90vh]` collapses too short on 375px | `Hero.jsx` L7 | 🟡 Medium |
| About section image 500px tall on mobile = overflow | `page.js` L59 | 🟡 Medium |
| "View All Calendar →" only on desktop, hidden on mobile | `page.js` L81 | 🟡 Medium (duplicate shown on L96, but no arrow) |
| Footer likely not tested at 375px | `Footer.jsx` | 🟡 Estimated |

### Tasks
- [ ] **2.5-C1** 🔴 **CRITICAL**: Implement mobile menu open/close state in `Header.jsx` (drawer or dropdown)
- [ ] **2.5-C2** Fix hero height on mobile — use `min-h-[500px] sm:h-[90vh]` or similar
- [ ] **2.5-C3** Constrain About image height on mobile: `max-h-[300px] lg:h-[500px]`
- [ ] **2.5-C4** Audit Footer at 375px — verify all columns stack correctly
- [ ] **2.5-C5** Add responsive 375px, 768px, 1024px, 1440px screenshots as acceptance criteria

---

## ♿ Accessibility — **GAPS IDENTIFIED**

UI/UX Pro Max pre-delivery checklist requires:
- No emojis as icons (use SVG) ✅ (we use Material Symbols)
- `cursor-pointer` on all clickable elements — partial
- Hover states with 150–300ms transitions — ✅ (most have `transition-all`)
- WCAG 4.5:1 contrast — needs verification (see Color section)
- Focus states visible — **not verified**
- `prefers-reduced-motion` — **not implemented**

### Tasks
- [ ] **2.5-D1** Add `focus-visible:ring-2 focus-visible:ring-accent` to all interactive elements (nav links, buttons, cards)
- [ ] **2.5-D2** Add `aria-label` to hamburger button (currently ✅ has it, but button does nothing)
- [ ] **2.5-D3** Add `alt` text audit — `siteConfig.aboutSnippet.imageAlt` — verify config provides real value
- [ ] **2.5-D4** Implement `@media (prefers-reduced-motion: reduce)` in globals.css to disable transforms/transitions
- [ ] **2.5-D5** Add `cursor-pointer` to EventCard, MemberCard, and CTA buttons
- [ ] **2.5-D6** Ensure all `<section>` elements have appropriate ARIA roles or `aria-labelledby`

---

## ⚡ Performance / Polish — **LOW RISK, HIGH IMPACT**

UI/UX Pro Max community landing pattern recommends: large sections (48px+ gaps), animated patterns, bold hover, scroll-snap, 200–300ms transitions.

Current site gaps:

| Issue | Location |
|-------|---------|
| No scroll animations (fade-in on scroll) | All sections |
| No `loading="lazy"` on About image | `page.js` L56 |
| No `sizes` prop on `<img>` | `page.js` L56–59 |
| `material-icons` font loaded without `&display=swap` timeout fallback | `layout.js` L38 |
| No Open Graph image defined | `layout.js` L26-30 |

### Tasks
- [ ] **2.5-E1** Add `loading="lazy"` and `sizes` to About `<img>` (or convert to `next/image`)
- [ ] **2.5-E2** Convert About image to `<Image>` from `next/image` for automatic optimization
- [ ] **2.5-E3** Add subtle scroll-reveal animation (CSS `@keyframes` fadeInUp, triggered via IntersectionObserver or `animate-on-scroll` class)
- [ ] **2.5-E4** Add `og:image` to metadata in `layout.js` (use `/public/og-image.jpg`)
- [ ] **2.5-E5** Add `twitter:card` metadata for social sharing
- [ ] **2.5-E6** Add `link rel="preconnect"` for Google Fonts in `layout.js <head>`

---

## 🧩 Component Architecture — **CLEAN, MINOR GAPS**

Config-driven architecture is already implemented (all text from `siteConfig`). 

| Component | Status | Notes |
|-----------|--------|-------|
| `Hero.jsx` | ✅ Clean | No hardcoded text |
| `Header.jsx` | ⚠️ Mobile broken | Nav links static, no mobile toggle logic |
| `StatsBar.jsx` | ✅ | Pulls from `siteConfig.stats` |
| `EventCard.jsx` | ✅ | Well-structured |
| `MemberCard.jsx` | ✅ | Has `variant` prop — extensible |
| `CTASection.jsx` | ✅ | Fully reusable with props |
| `Footer.jsx` | ⚠️ Unverified | Largest file (10.5KB) — may have hardcoded strings |

### Tasks
- [ ] **2.5-F1** Audit `Footer.jsx` for hardcoded strings — move all to `siteConfig`
- [ ] **2.5-F2** Add `config/site.js` validation — throw at build time if required keys are missing

---

## 📋 Master Checklist — Priority Order

### 🔴 Critical (Block Phase 3)
- [ ] 2.5-C1 — Mobile hamburger menu implementation
- [ ] 2.5-D1 — Focus states for keyboard accessibility

### 🟠 High (Complete before first lead clone)
- [ ] 2.5-A1 — Add body/muted color tokens
- [ ] 2.5-A2 — Verify WCAG gold-on-white contrast
- [ ] 2.5-B1 — Consistent `font-display` on h2 headings
- [ ] 2.5-C2 — Fix hero height on mobile
- [ ] 2.5-C3 — Constrain About image height on mobile
- [ ] 2.5-E2 — Convert About `<img>` to `next/image`
- [ ] 2.5-F1 — Footer hardcoded string audit

### 🟡 Medium (Polish pass post-clone)
- [ ] 2.5-B2 — Standardize heading scale
- [ ] 2.5-D4 — prefers-reduced-motion
- [ ] 2.5-D5 — cursor-pointer sweep
- [ ] 2.5-D6 — Section ARIA roles
- [ ] 2.5-E1 — img lazy + sizes
- [ ] 2.5-E3 — Scroll-reveal animation
- [ ] 2.5-E4 — og:image metadata
- [ ] 2.5-E6 — preconnect for fonts

### 🟢 Nice-to-Have
- [ ] 2.5-A3 — Verify hero overlay contrast
- [ ] 2.5-B3 — Font-size tokens
- [ ] 2.5-D3 — alt text audit
- [ ] 2.5-E5 — twitter:card metadata
- [ ] 2.5-F2 — siteConfig validation at build

---

## UI/UX Pro Max Recommended Design System (Reference)

*Matched pattern: "Community/Forum Landing" — Adjust for civic/authority brand:*

| Token | Community Default | KCC Override |
|-------|-----------------|-------------|
| Primary | `#7C3AED` | `#1A2E4A` ✅ |
| CTA | `#22C55E` | `#C8971A` ✅ |
| Heading font | Rubik | Playfair Display ✅ |
| Body font | Nunito Sans | Inter ✅ |
| Gap scale | 48px+ sections | 96px (py-24) ✅ |
| Hover animation | 200–300ms | 150–300ms ✅ |
| Hover effect | Bold color shift | scale-105 + color ✅ |
